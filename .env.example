# marquee — environment
# Express API / production server port
MARQUEE_API_PORT=3401
# Vite dev client port
VITE_PORT=3400
# nedbd — the provenance spine (build step 3 onward)
NEDB_URL=http://127.0.0.1:7070
NEDB_DB=marquee
